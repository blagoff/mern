// Import Actions
import {  } from './PostsActions';

// Initial State
const initialState = {};

const PostsReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default PostsReducer;
